Select component with options passed as props.

```jsx
import Select from "./Select";
<Select options={["one piece", "naruto", "code geass"]} />;
```

<br/>
Customised dropdown menu. 

```jsx
import Select from "./Select";
<Select
  options={["one piece", "naruto", "code geass"]}
  selected="naruto"
  background="#262626"
  width="50%"
/>;
```
